import fetcher from './fetcher';
import getLimit from './getLimit';
import {
  getCompletedTodos,
  getDeletedPosts,
  getFavoritePosts,
  isFavoritePost,
  toggleFavoritePosts,
} from './storage';

export {
  fetcher,
  getCompletedTodos,
  getDeletedPosts,
  getFavoritePosts,
  getLimit,
  isFavoritePost,
  toggleFavoritePosts,
};
