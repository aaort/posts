import fetcher from './fetcher';
import getLimit from './getLimit';
import {
  getCompletedTodos,
  getDeletedPosts,
  getFavoritePosts,
} from './storage';

export {
  fetcher,
  getCompletedTodos,
  getDeletedPosts,
  getFavoritePosts,
  getLimit,
};
