import fetcher from './fetcher';
import getLimit from './getLimit';
import {
  deletePosts,
  getCompletedTodos,
  getDeletedPosts,
  getFavoriteAlbums,
  getFavoritePosts,
  isFavoriteAlbum,
  isFavoritePost,
  toggleFavoriteAlbums,
  toggleFavoritePosts,
} from './storage';

export {
  deletePosts,
  fetcher,
  getCompletedTodos,
  getDeletedPosts,
  getFavoriteAlbums,
  getFavoritePosts,
  getLimit,
  isFavoriteAlbum,
  isFavoritePost,
  toggleFavoriteAlbums,
  toggleFavoritePosts,
};
