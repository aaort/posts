import fetcher from './fetcher';
import getLimit from './getLimit';
import {
  deletePosts,
  getCompletedTodos,
  getDeletedPosts,
  getFavoritePosts,
  isFavoritePost,
  toggleFavoritePosts,
} from './storage';

export {
  deletePosts,
  fetcher,
  getCompletedTodos,
  getDeletedPosts,
  getFavoritePosts,
  getLimit,
  isFavoritePost,
  toggleFavoritePosts,
};
