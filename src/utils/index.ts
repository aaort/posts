import fetcher from './fetcher';
import getLimit from './getLimit';
import {
  getCompletedTodos,
  getDeletedPosts,
  getFavoritePosts,
  isFavoritePost,
  toggleFavoritePost,
} from './storage';

export {
  fetcher,
  getCompletedTodos,
  getDeletedPosts,
  getFavoritePosts,
  getLimit,
  isFavoritePost,
  toggleFavoritePost,
};
