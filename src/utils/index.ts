import fetcher from './fetcher';
import getLimit from './getLimit';
import {
  toggleDeletedPosts,
  getCompletedTodos,
  getDeletedPosts,
  getFavoriteAlbums,
  getFavoritePosts,
  isFavoriteAlbum,
  isFavoritePost,
  toggleFavoriteAlbums,
  toggleFavoritePosts,
} from './storage';

export {
  toggleDeletedPosts,
  fetcher,
  getCompletedTodos,
  getDeletedPosts,
  getFavoriteAlbums,
  getFavoritePosts,
  getLimit,
  isFavoriteAlbum,
  isFavoritePost,
  toggleFavoriteAlbums,
  toggleFavoritePosts,
};
