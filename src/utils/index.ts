import fetcher from './fetcher';
import getLimit from './getLimit';
import {
  getCompletedTodos,
  getDeletedAlbums,
  getDeletedPosts,
  getFavoriteAlbums,
  getFavoritePosts,
  isFavoriteAlbum,
  isFavoritePost,
  toggleDeletedAlbums,
  toggleDeletedPosts,
  toggleFavoriteAlbums,
  toggleFavoritePosts,
} from './storage';

export {
  fetcher,
  getCompletedTodos,
  getDeletedAlbums,
  getDeletedPosts,
  getFavoriteAlbums,
  getFavoritePosts,
  getLimit,
  isFavoriteAlbum,
  isFavoritePost,
  toggleDeletedAlbums,
  toggleDeletedPosts,
  toggleFavoriteAlbums,
  toggleFavoritePosts,
};
