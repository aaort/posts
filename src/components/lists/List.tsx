import Column from '@/components/common/Column';
import { styled } from '@/theme';

const List = styled(Column, {
  maxWidth: '70%',
  alignItems: 'center',
  gap: '$3',
  mx: 'auto',
  mb: '$2',
});

export default List;
